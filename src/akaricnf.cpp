#include "akaricnf.h"

void assertRow(const CNF& cnf, const vector<int>& row, int s, int e) {
	
}

void calcAkari(const CNF& cnf, const Akari& akari) {
	// rows must have at most 1
	
	// cell (rows OR columns) must be true
	// special tiles must have:
	// 0
	// 1
	// 2
	// 3
	// 4
}
